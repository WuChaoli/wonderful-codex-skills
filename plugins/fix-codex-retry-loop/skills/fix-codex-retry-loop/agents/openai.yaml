interface:
  display_name: "修复 Codex 重试循环"
  short_description: "诊断本机网络代理，并在确认后安全写入 Codex .env"
  default_prompt: "使用 $fix-codex-retry-loop 诊断 Codex 持续 retry 或 reconnecting；需要代理配置时先展示变更，确认后写入 $CODEX_HOME/.env。"
